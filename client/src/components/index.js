import Card from "./Card";
import FormField from "./FormField";
import Loader from "./Loader";

export{
    Card,
    FormField,
    Loader
}

// To export all the 3 components as a single object in the HomePage